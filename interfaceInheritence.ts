interface UserInfo {
    name: string
    email: string
}

function printUserInfo(info: UserInfo) {


}

// extends means inheritence the class
//implements means inheritence the interface
//second declaration of interface with same name is allowed

